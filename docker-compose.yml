version: '3.8'
services:
  sovet.id:
    image: sovet.id:1.2
    container_name: sovet.id
    build:
      context: docker
      dockerfile: Dockerfile
    ports:
      - '8081:80'
    volumes:
      - .:/app
    command: >
      sh -c "php artisan serve --host=0.0.0.0 --port=80"
